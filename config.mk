# $Ragnarok$
# Modify to fit your setup.

# Path where EOD's executables will be installed. **Do not use /bin or
# /usr/bin on systems that have RCS already installed**. /lib or
# /usr/lib is more appropriate.
EOD_PATH	= /usr/bin

# Path where the RCS tools should be installed. Systems that already
# have RCS installed should define this as /usr/bin. If the binaries
# are already there, they won't be overwritten.
RCS_PATH	= /usr/lib

# 
